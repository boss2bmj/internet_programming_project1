




<%= form_for @imageforhome, html: {multipart: true } do |f| %>
  <% if @imageforhome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@imageforhome.errors.count, "error") %> prohibited this imageforhome from being saved:</h2>

      <ul>
      <% @imageforhome.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="section no-pad-bot">
    <div class="container">
      <div class="row main-text-color">
        <form action="#">
          <div class="file-field input-field ">
            <div class="btn blue darken-4">
              <span>File</span>
              <input type="file" multiple> <%= f.file_field :image %> </input>
            </div>
            <div class="file-path-wrapper ">
              <input class="file-path validate " type="text" placeholder="Upload one or more files">
            </div>
          </div>
        </form>
        <br>

        <div class="field">
          <label for="product_name">Image Name</label>
          <%= f.text_field :image_name %>
        </div><br>

        <div class="input-field">
          <%= f.label :description %><br><br>
          <%= f.text_area :description, class: "materialize-textarea" %>
        </div><br>


        <div class="actions btn waves-effect waves-light blue darken-4" type="submit" name="commit">
          <%= f.submit %>
          <i class="fa fa-paper-plane"></i>
        </div>
        <% end %>
      </div>
    </div>
  </div>
